// This local module ships a native VisionCamera frame processor plugin
// ("detectHands") implemented with Apple Vision (VNDetectHumanHandPoseRequest).
// It has no JavaScript surface — the plugin is consumed from
// src/vision/detectHands.ts via VisionCameraProxy.initFrameProcessorPlugin.
export {};
